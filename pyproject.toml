[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ecode"
version = "1.0.0"
dependencies = [
    "rich",
    "langchain-core",
    "langgraph",
]

[project.scripts]
ecode = "cli.chat:main"